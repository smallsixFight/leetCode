### Pow(x, n)（Pow(x, n)）
[原题来源：力扣（LeetCode）](https://leetcode-cn.com/problems/powx-n/)

#### 题目
实现`pow(x, n)`，即计算 x 的 n 次幂函数。

示例 1：
```
输入: 2.00000, 10
输出: 1024.00000
```
示例 2：
```
输入: 2.10000, 3
输出: 9.26100
```
示例 3：
```
输入: 2.00000, -2
输出: 0.25000
解释: 2-2 = 1/22 = 1/4 = 0.25
```
**说明**  
- -100.0 < x < 100.0
- n 是 32 位有符号整数，其数值范围是 [−2^31, 2^31 − 1] 。

#### 思路
一开始的思路是从 0 不断迭代到 n，每次都`res *= x`，然后根据`n`是否为负数决定是否取倒数；不过感觉不会这么简单才对，看了下题目标签有二分查找，于是再次思考。

如何二分呢？首先，`x`肯定跟二分无关，那么只能是`n`了，然后...然后我查了下资料找到了下面的公式。

![binary_pow.jpg](https://i.loli.net/2020/03/21/R5ny3D7OIT1fxCw.jpg)

上面的公式可以想到一个二分递归的方法，找出`a^(n/2)`或`a^((n-1)/2)`来获取当前的值；当 n 为负数的时候，需要取倒数，要做除法。

#### 题外话
拥有足够的精力去做题才能好好做，昨晚没睡好，今天上班一边想着上图的公式还是一点思路都没有。下班洗完澡看了两集剧，精神好好的，思路没一会儿就出来了。

另外，`MyPowTwo()`是一种迭代的写法，当前没能理解，当作之后回顾的思考题吧。

#### 伪代码
```
MY-POW(x, n)
    if n == 0
        return 1
    pre = MY-POW(x, n /2)
    if n % 2 == 0
        return pre *pre
    if n > 0
        return pre *pre *x
    return pre *pre /x      // n 为负数
```